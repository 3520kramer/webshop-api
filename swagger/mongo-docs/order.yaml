paths:
  # Create product - works
  /order/{userId}:
    post:
      tags:
        - Orders
      summary: 'Roles required: Employee, Developer or Admin'
      description: 'This is the route for creating a new order'
      produces:
      - application/json
      parameters:         # request parameters
      - in: path        # request header
        name: userId
        schema:
        type: string
        required: true

      - in: body          # request body
        name: create order     # name of request, can be any name
        required: true   # can also be true depending on user preference
        schema:           # Schema definition
          $ref: '#/definitions/CreateOrder' 

      responses:          # server responses
        201:
          description: ''
        500:
          description: ''


  # find one product - works
  /order/{orderId}:  
    get:
      tags:
      - Orders
      summary: 'Roles required: Employee, Developer or Admin'
      description: 'Numeric ID of the user to get'
      parameters:
        - in: path
          name: orderId
          schema:
            type: string
          required: true
      responses:          # server responses
        200:
          description: ''
        500:
          description: ''


# Schema defination for request body
definitions:        
  CreateOrder:
    type: object
    properties:
      order:
        type: object
        properties:
          comment:
            type: string
            example: "could you slip it over the fence"
          created:
            type: date
            example: 

          products:
            type: array
            items:
              type: object  
              properties:
                name:
                  type: string
                  example: "wizard hat"
                size:
                  type: "string"
                  example: "l"
                brand:
                  type: "string"
                  example: "Maiar"
                color: 
                  type: "string"
                  example: "gray"
                price: 
                  type: "integer"
                  example: "125"
                category: 
                  type: "string"
                  example: "hats"      
                materials: 
                  type: array
                  example: [{"material": "wool"}, {"material": "magic"}]
                description: 
                  type: "string"
                  example: "A wizard hat is never late"
              example:
                - type:

          shipment:
            type: object
            properties:
              shipper: 
                type: "string"
                example: "Post Nord"
              deliveryType: 
                type: "string"
                example: "home delivery"
              shipmentPrice: 
                type: "number"
                example: "123"    
              
          orderStatus:
            type: "string"
            example: "NOT PROCESSED"      
          shippedDate:
            type: "date"
            example:      
          poBoxDelivery:
            type: integer
            nullable: true
              
          customerBilling:
            type: object
            properties:
              firstName:
                type: string
                example: "Jennifer"
              lastName:
                type: string
                example: "Chandler"
              street:
                type: string
                example: "8650 Michael Grove"
              email:
                type: string
                example: "gjoseph@gmail.com"
              phone:
                type: string
                example: "40949002"
              city:
                type: string
                example: "Roskilde"
              citiesPostalCode:
                type: number
                example: 1000
              countriesISO:
                type: string
                example: "DK"
              country:
                type: string
                example: "Denmark"

          customerDelivery:
            type: object
            properties:
              firstName:
                type: string
                example: "Jennifer"
              lastName:
                type: string
                example: "Chandler"
              street:
                type: string
                example: "8650 Michael Grove"
              email:
                type: string
                example: "gjoseph@gmail.com"
              phone:
                type: string
                example: "40949002"
              city:
                type: string
                example: "Roskilde"
              citiesPostalCode:
                type: number
                example: 1000
              countriesISO:
                type: string
                example: "DK"
              country:
                type: string
                example: "Denmark"

                        
